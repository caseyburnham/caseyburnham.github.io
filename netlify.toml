[build]
  command = "npm run build:deploy"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  autoLaunch = false
  command = "npm run watch:dev"
  framework = "#static"
  port = 8888
  publish = "."
